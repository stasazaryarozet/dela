# Универсальная архитектура интеграции

**Принципы:**
- **ZF (Zero Formalization):** Никаких жестких целей, никаких "для чего"
- **MA (Maximum Access):** Максимум доступа ко всем провайдерам
- **IA (Infinite Adaptivity):** Адаптация на ходу к любым задачам
- **MI (Maximum Information):** Весь контекст от всех провайдеров доступен

---

## Мета-паттерн Gate

**Не "что делать", а "что возможно".**

```python
class UniversalGate:
    """Любой провайдер"""
    
    def connect(self):
        """Подключение = максимум прав доступа"""
        pass
    
    def capabilities(self):
        """Что ВООБЩЕ можно делать с этим провайдером"""
        return {
            'read': [...],   # Всё, что можно прочитать
            'write': [...],  # Всё, что можно записать  
            'listen': [...], # Всё, на что можно подписаться (webhooks)
            'search': [...], # Всё, что можно искать
            'export': [...]  # Всё, что можно экспортировать
        }
    
    def do(self, action, **params):
        """Универсальный метод: делай что угодно"""
        pass
    
    def substance(self):
        """Экспорт ВСЕГО доступного"""
        pass
```

---

## Реализация

### Каждый Gate = максимум API

**НЕ:**
```python
# Узкий, целевой подход
def send_consultation_reminder(client_email):
    gmail.send(to=client_email, subject="Напоминание о консультации")
```

**ДА:**
```python
# Широкий, универсальный подход
google_gate = GoogleGate()

# Весь Gmail API доступен:
# - send, read, labels, search, filters, drafts, attachments,
# - threads, history, watch, batch, import, delete, trash, ...

# Используй что нужно СЕЙЧАС, остальное — доступно для ЛЮБЫХ будущих задач
```

---

## Auto-Discovery провайдеров

**Создать:** `.gates/discover.py`

**Что делает:**
- Сканирует все аккаунты (Google, Meta, Apple, Yandex)
- Находит ВСЕ подключенные сервисы
- Автоматически создает Gates для каждого
- Получает максимальные права (OAuth with all scopes)

**Пример:**

```bash
python3 .gates/discover.py --account o.g.rozet@gmail.com
```

**Результат:**
```
🔍 Обнаружено провайдеров для o.g.rozet@gmail.com:

Google:
  ✅ Gmail
  ✅ Calendar  
  ✅ Drive
  ✅ Contacts
  ✅ Sheets
  ✅ Docs
  ✅ Forms
  ✅ YouTube (канал не создан, но API доступен)
  ✅ Maps API

Meta (связанные аккаунты):
  ⚠️ Instagram @olga.rozet (требует подключение к Page)
  ✅ Facebook Page "Home-resurs"
  🔨 WhatsApp (номер не подключен)
  🔨 Messenger (Page есть, Product не добавлен)

Apple ID:
  🔨 iCloud Drive
  🔨 iCloud Calendar
  🔨 iCloud Notes
  🔨 iCloud Photos

Яндекс:
  ✅ Яндекс.Диск (используется)
  ✅ SpeechKit (используется)
  🔨 Яндекс.Метрика
  🔨 Яндекс.Карты

Сторонние (через OAuth):
  🔨 Zoom (требует авторизация)
  🔨 Slack
  🔨 Notion
  🔨 Airtable

Создать Gates для всех? (y/n)
```

---

## Auto-Connect (Zero Human Effort)

**Принцип:** Система САМА получает максимальные права от всех провайдеров

**Механизм:**

1. **Единая OAuth сессия:**
   - Запустить `python3 .gates/autoconnect.py`
   - Откроется браузер
   - Вы входите в Google/Meta/Apple/Yandex ОДИН РАЗ
   - Система автоматически:
     - Находит все доступные API
     - Запрашивает максимальные scopes
     - Получает токены
     - Создает Gates
     - Настраивает webhooks

2. **Результат:**
   - Все провайдеры интегрированы
   - Все токены сохранены
   - Все webhooks активны
   - Substance Export настроен

**Время:** 5 минут (один вход → всё автоматически)

---

## Substance = ВСЁ

**Не "данные для задачи X", а "ВСЁ, что есть":**

```json
{
  "timestamp": "2025-11-11T19:30:00Z",
  "providers": {
    "google_gmail": {
      "emails_count": 15234,
      "labels": [...],
      "recent_threads": [...],
      "contacts_mentioned": [...]
    },
    "google_calendar": {
      "events_upcoming": [...],
      "events_past_30_days": [...],
      "busy_times": [...]
    },
    "google_drive": {
      "files_count": 8942,
      "recent_files": [...],
      "shared_with_me": [...]
    },
    "telegram": {
      "chats": [...],
      "messages_archived": 2341,
      "bot_status": "active"
    },
    "instagram": {
      "posts": [...],
      "comments": [...],
      "followers": 8419,
      "engagement_rate": 3.2
    },
    "zoom": {
      "upcoming_meetings": [...],
      "past_meetings": [...],
      "recordings": [...]
    },
    "github": {
      "repos": [...],
      "commits_last_week": 47,
      "open_issues": [...]
    },
    "notion": {
      "pages": [...],
      "databases": [...]
    }
    // ... ВСЕ остальные провайдеры
  },
  "cross_provider_insights": {
    "email_mentioned_in_drive": [...],
    "calendar_events_with_zoom_links": [...],
    "instagram_comments_from_gmail_contacts": [...],
    "github_commits_mentioned_in_telegram": [...]
  }
}
```

---

## Адаптивность = Граф возможностей

**Не:**
- "Для продажи консультаций нужны Instagram + Telegram"

**Да:**
- "Доступны: 50+ провайдеров. Используй любые для любых задач."

**Пример адаптации на ходу:**

```
Интенция: "Хочу продать консультации"

Система (НЕ спрашивая):
  1. Находит: Instagram (8419 подписчиков) → используем
  2. Находит: Telegram бот → создаем канал
  3. Находит: Gmail → настраиваем автоответы
  4. Находит: Notion → создаем CRM
  5. Находит: Stripe → добавляем оплату
  6. Находит: Google Analytics → трекаем конверсии
  
Интенция изменилась: "Хочу написать книгу"

Система (адаптируется):
  1. Находит: Notion → создаем структуру книги
  2. Находит: Google Docs → пишем главы
  3. Находит: GitHub → версионирование
  4. Находит: Telegram → канал для читателей
  5. Находит: YouTube → видеоотрывки
```

**Максимум возможностей → адаптация к ЛЮБОЙ интенции.**

---

## Реализация (сейчас)

**Создаю:**

### 1. `autoconnect.py` — автоматическое подключение ВСЕХ провайдеров

**Запуск:**
```bash
python3 .gates/autoconnect.py
```

**Процесс:**
- Открывает браузер
- Вы входите в Google/Meta/Apple/Yandex
- Система автоматически находит все API
- Запрашивает максимальные права
- Создает Gates
- Настраивает webhooks
- → Готово

### 2. `discover.py` — обнаружение доступных провайдеров

**Показывает что УЖЕ доступно, что можно подключить**

### 3. Template для Universal Gate

**Каждый Gate:**
- Максимум API (все endpoints)
- Минимум специфичности (универсальные методы)
- Автоматический export_substance()
- Webhooks setup

### 4. `unified_substance.py` — агрегация от ВСЕХ Gates

**Один файл = ВСЯ информация от ВСЕХ провайдеров**

---

**Создаю все 4 скрипта?**
